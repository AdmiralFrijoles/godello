# Rust / Cargo
/target/
**/*.rs.bk
*.pdb

# Cargo.lock is kept for binary crates (this workspace produces a binary),
# so it is not ignored.

# JetBrains RustRover and IntelliJ.
# Cargo is the source of truth, so ignore the whole editor folder.
/.idea/
*.iml
*.iws

# OS / editor cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
*~
*.swp
*.swo

# Godello local runtime data that might land in-tree during dev
/.godello/
*.log
